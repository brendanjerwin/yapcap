# Ralph Progress

Feature: multi-process-runtime-sync
Started: 2026-06-15T16:14:14+02:00

## 2026-06-15T16:20:07+02:00 - 001-shared-runtime-control-state

- Implemented: Added versioned shared runtime/control COSMIC config entries, loaded shared runtime/control at startup, subscribed to shared updates, persisted runtime state through shared config, and removed active `snapshots.json` runtime cache code.
- Files changed: `src/shared_state.rs`, `src/runtime.rs`, `src/app/mod.rs`, `src/app/provider_actions.rs`, `src/config.rs`, `src/model.rs`, `src/error.rs`, `src/main.rs`, `src/cache.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/001-shared-runtime-control-state.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: COSMIC config watchers require shared entry payloads to implement `PartialEq`; shared runtime can reuse the existing `AppState` reconciliation path while later owner-lock work restricts which process is allowed to publish runtime updates.

## 2026-06-15T16:25:10+02:00 - 002-refresh-owner-lock

- Implemented: Added refresh owner election with a Unix file lock in the YapCap state directory, owner/non-owner/read-only startup logging, non-owner blocking takeover wait, owner handle lifetime retention, and shared refresh request clearing on owner startup/takeover.
- Files changed: `src/refresh_owner.rs`, `src/app/mod.rs`, `src/shared_state.rs`, `src/config.rs`, `src/main.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/002-refresh-owner-lock.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: The owner handle can be carried through the app message system by wrapping the locked file in an `Arc`, which keeps the lock alive while preserving `Message: Clone`.

## 2026-06-15T16:30:40+02:00 - 003-owner-automatic-refresh

- Implemented: Routed startup/timer refresh through the refresh owner, filtered automatic refresh to enabled providers with missing or stale selected-account runtime data, persisted shared refreshing state before provider work, and kept non-owner timer ticks inert.
- Files changed: `src/app/mod.rs`, `src/app/refresh.rs`, `src/app/tests.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/003-owner-automatic-refresh.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: The existing refresh task builder can stay as the manual/all-provider path while automatic refresh uses a narrower prefilter, which keeps owner-only timer behavior separate from later shared-control request work.

## 2026-06-15T16:37:27+02:00 - 004-user-refresh-requests

- Implemented: Routed Refresh now through shared control requests for enabled providers, owner-only execution of observed requests, duplicate in-flight request suppression, and provider request consumption after runtime publication.
- Files changed: `src/app/mod.rs`, `src/app/provider_actions.rs`, `src/app/tests.rs`, `src/shared_state.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/004-user-refresh-requests.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: Manual refresh can reuse the existing provider refreshing/final runtime publication path once shared control is the cross-process intent channel; cleanup is safest on provider refresh completion so duplicate requests during an in-flight refresh are naturally consumed by the final runtime write.

## 2026-06-15T16:42:02+02:00 - 005-provider-selection-sync

- Implemented: Persisted selected-provider tab changes through durable config, applied selected-provider config updates without sharing popup-local state, and wrote provider-selected shared refresh requests when the selected enabled provider has missing or stale selected-account runtime data.
- Files changed: `src/app/mod.rs`, `src/app/provider_actions.rs`, `src/app/refresh.rs`, `src/app/tests.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/005-provider-selection-sync.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: Provider selection can share the automatic refresh freshness predicate so timer refresh and stale-on-select requests stay aligned while local popup route/open state remains process-local.

## 2026-06-15T16:51:31+02:00 - 006-login-account-actions-safe

- Implemented: Routed login, re-authentication, account selection, provider enablement, and account deletion through shared refresh requests while keeping shared runtime publication and cleanup owner-only.
- Files changed: `src/app/login.rs`, `src/app/mod.rs`, `src/app/provider_actions.rs`, `src/app/refresh.rs`, `src/app/tests.rs`, `src/providers/adapters.rs`, `src/providers/claude/login.rs`, `src/providers/codex/login.rs`, `src/shared_state.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/006-login-account-actions-safe.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo check`; `just check`; `cargo test`.
- Learnings: Config reconciliation needs refreshable placeholder account state for newly selected or newly added accounts; otherwise the refresh scheduler treats not-yet-refreshed accounts like action-required failures and skips owner refresh.

## 2026-06-16T09:00:18+02:00 - 007-multi-process-diagnostics

- Implemented: Added short process ids, startup/takeover diagnostics with config/runtime/control versions and generations, shared runtime/control load/write/watch logs, refresh request creation/owner observation/skips, provider refresh start/finish/error logs, runtime fallback logs, and helper-level process id formatting coverage.
- Files changed: `src/refresh_owner.rs`, `src/shared_state.rs`, `src/runtime.rs`, `src/app/mod.rs`, `src/app/provider_actions.rs`, `src/app/refresh.rs`, `docs/spec.md`, `issues/multi-process-runtime-sync/issues/007-multi-process-diagnostics.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: Shared runtime generations need to advance on every owner write for observed runtime logs to be useful across multiple applet processes.

## 2026-06-16T09:08:02+02:00 - 008-docs-qa-plan

- Implemented: Documented the multi-process applet model in the product spec, added two-display runtime sync QA coverage with expected owner/non-owner log evidence, updated old `snapshots.json` QA expectations, and isolated refresh scheduling tests from demo-mode environment leakage.
- Files changed: `docs/spec.md`, `docs/qa.md`, `src/app/refresh.rs`, `issues/multi-process-runtime-sync/issues/008-docs-qa-plan.md`, `issues/multi-process-runtime-sync/progress.txt`.
- Checks run: `cargo fmt`; `cargo test`; `cargo check`; `just check`.
- Learnings: Refresh scheduling tests share the same demo-mode environment gate as runtime code, so tests that assert scheduling must hold the env lock and explicitly remove `YAPCAP_DEMO`.
